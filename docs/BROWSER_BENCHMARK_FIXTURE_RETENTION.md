# 大量フォルダ計測データの保管メモ（2026-09-22）

対象は `out/todo19-warm10000` と `out/todo19-warm1000`。いずれも通常のアプリ起動や機能テストには不要な、TODO19の性能調査用データである。ユーザーの画像ではなく、`scripts/benchmark_browser_warm_startup.py` が高精細な合成画像から作ったJPEG・PNG・AVIF・JXL・WebP・PDF・CBZ・フォルダと、その保存済みサムネイルを含む。`prepare` は既存の対象ディレクトリを上書きしない。

## 作成・使用履歴

- 2026-09-19 21:27–21:29 JST: 10,000件のデータを生成。保存済みサムネイルがある大量フォルダの起動と履歴移動で、初期表示やキャッシュの整理が遅い問題を比較するため。詳細は `docs/BROWSER_WARM_STARTUP_INVESTIGATION.md` と `docs/BROWSER_NAVIGATION_RESPONSIVENESS.md`。
- 2026-09-19 21:29–22:28 JST: ワーカー数、整理期限到来時、Viewerとの並行動作、履歴移動などを比較。結果は `out/todo19-*.json` 等に保存。
- **最後に確認できた利用: 2026-09-22 00:18 JST**。1.0.10のBrowser起動時走査の旧実装／改善実装を同じ10,000件のデータで比較。`out/startup-review-legacy.json`、`out/startup-review-current.json` と計測用 `profile` の更新時刻で確認。これより後の利用記録は2026-09-22の調査時点で見つからなかった。Windowsの最終アクセス日時だけで実際の読取有無は判定しない。

## 容量と今後の扱い

2026-09-22時点で `todo19-warm10000` はファイルサイズの単純合計が約91.1 GiB。ただしNTFSハードリンクの重複を除いたファイル実体のサイズ合計は概算約1.45 GiBであり、91 GiBを個別に書き込んだものではない。実際のディスク割当量はこれらの数値と一致するとは限らない。

ユーザーの判断で**当面保管**する。現在予定されている計測はなく、通常のテストには使わない。容量が問題になったときは削除してよい候補だが、削除前にユーザーへ確認する。**既存データを使わずに同規模を自動再生成しない**。今後の計測で使用した場合は、日時・用途・結果ファイルをこのメモに追記する。

最後に確認できた利用から**2か月以上**新しい利用記録がなければ、削除するかユーザーに尋ねる。未回答の間は同じ問い合わせを繰り返さず、自動削除もしない。最終アクセス日時は信頼できない場合があるため、このメモ、計測結果、計測用 `profile` の記録を照合し、不明なら「利用記録を確認できない」と伝える。
