# Codex Repository Rules

- 変更前に既存コードとドキュメントを読む。
- UIスレッドで画像デコードや書庫展開を行わない。
- 古い非同期読み込み結果が現在表示へ混入しないよう、世代番号または要求IDを維持する。
- `PageModel`へGUI描画処理を入れない。
- `ViewerWidget`へファイル探索や設定保存を入れない。
- 依存ライブラリを追加する場合は、理由とライセンスを報告する。
- AGPL依存を勝手に追加しない。
- ZipPlaのソースコードをコピーしない。
- Windowsと日本語ファイル名を前提にする。
- 設定やキャッシュをユーザーの画像フォルダへ書き込まない。
- 変更後は構文チェックとテストを実行する。
- コミット／Pushはユーザーの明示指示がない限り行わない。
